Redux 是Flux的一种实现

1.Redux规定应用只能拥有一个store
2.Reducer 数据的改变通过纯函数Reducer完成

在Redux中，我们应该通过reducer直接返回一个新的state。

在标准的MVC框架中,数据可以在UI组件和存储之间双向流动

Redux严格限制了数据只能在一个方向上流动。

在Redux中,所以数据(state)被保存在一个被称为 store的容器中

store本质上是一个状态树,保存了所有对象的状态。任何UI组件都可以直接从store访问特定对象的状态。

